# This is the __init__ for the attrib's package
# it just auto imports a++ll the modules in the package when the package is imported
import attribs.damage as damage
import attribs.memory as memory
import attribs.model as model
import attribs.personality as personality
import attribs.thread as thread
import attribs.timeLine as timeLine
